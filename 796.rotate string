class Solution:
    def rotateString(self, s, goal):
        if len(s)!=len(goal):
            return False
        for i in range(len(s)):
            if goal[i]!=s[i]:
                return True
            if goal[i] not in s[i]:
                return True
            else:
                return False
