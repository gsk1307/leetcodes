class Solution:
    def thirdMax(self, nums):
        nums.sort()
        nums=list(set(nums))
        n=len(nums)
        if n<3:
            return nums[-1]
        else:
            return nums[-3]
