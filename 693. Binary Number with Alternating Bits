class Solution:
    def hasAlternatingBits(self, n):
        a=bin(n)[2:]
        
        for i in range(len(a)-1):
            if a[i]!=a[i+1]:
                return True
    
        return False
        
